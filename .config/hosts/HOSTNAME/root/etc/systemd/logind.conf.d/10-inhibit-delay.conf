# Give hypridle time to bring the session lock up before suspend.
# hypridle holds a sleep DELAY inhibitor and releases it only once the
# ext-session-lock surface reports locked (hyprland_lock_notify_v1).
# The 5s default expired every lid close, suspending the session UNLOCKED.
[Login]
InhibitDelayMaxSec=20
